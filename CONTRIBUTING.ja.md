# UnionAir へのコントリビュート

[English](CONTRIBUTING.md) | **日本語**

> **注記**: 本ドキュメントは [英語版 CONTRIBUTING](CONTRIBUTING.md) の翻訳です。内容に乖離がある場合は英語版が優先されます。

コントリビュートへの関心をお寄せいただきありがとうございます!

## プロジェクトの状態

UnionAir は**ベータ以前の実験的**プロジェクトです。API・エンドポイント・動作は後方互換性なしにいつでも変更・削除される可能性があります。リリース間で破壊的変更が入ることを前提としてください。

## Issue とプルリクエスト

UnionAir は現在、設計初期段階にあり、アーキテクチャや API 仕様が大幅に変更される可能性があります。

- バグ報告・機能要望・質問は [GitHub Issues](https://github.com/LeonAkasaka/UnionAir/issues) で受け付けています。
- 変更内容を Issue で相談する前にプルリクエストを作成しないでください。
- 現在プルリクエストを受け付けるのは、メンテナーと変更方針について合意済みの場合、または外部コントリビューション対象として明示された Issue に対応する場合のみです。
- 事前相談のないプルリクエストは、未公開の変更予定と競合する可能性があるため、詳細なレビューを行わずクローズする場合があります。

## 開発規約

開発規約はすべて [AGENTS.md](AGENTS.md) に集約されており、人間・AI を問わずすべてのコントリビューターに適用されます。要点:

- コードコメント・ドキュメント・コミットメッセージは**英語のみ**。日本語ドキュメントは英語原本の `.ja.md` 訳としてのみ存在します。
- API エンドポイントを追加・変更・削除した場合は、対応する `Documentation~/api/<category>.md` ページ、`[UnionAirEndpoint]` メタデータ、`CHANGELOG.md` を**同一コミットで**更新してください。
- 英語ドキュメントを変更した場合は、対応する `.ja.md` 訳を同一コミットで更新してください。
- 外部 NuGet 依存は追加しないでください — HTTP サーバは意図的に `HttpListener` ベースです。

## 既知の制約

- 現時点で**自動テストスイート・CI はありません**。変更は Unity 6000.0+ プロジェクトで手動検証してください([はじめに](Documentation~/index.ja.md) を参照)。
- リクエストボディの JSON パースは軽量な独自リーダー(`Editor/Utils/RequestBodyReader.cs`)で、深いネストにエッジケースがあります。
- レスポンスの JSON シリアライズはエンドポイントごとの手書き実装です。共有シリアライザへのリファクタリングは歓迎します。

## セキュリティ問題

セキュリティに関する問題は、公開 Issue ではなく [SECURITY.ja.md](SECURITY.ja.md) の手順に従って報告してください。
