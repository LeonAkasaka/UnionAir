# UnionAir へのコントリビュート

[English](CONTRIBUTING.md) | **日本語**

> **注記**: 本ドキュメントは [英語版 CONTRIBUTING](CONTRIBUTING.md) の翻訳です。内容に乖離がある場合は英語版が優先されます。

コントリビュートへの関心をお寄せいただきありがとうございます!

## プロジェクトの状態

UnionAir は**ベータ以前の実験的**プロジェクトです。API・エンドポイント・動作は後方互換性なしにいつでも変更・削除される可能性があります。リリース間で破壊的変更が入ることを前提としてください。

## Issue とプルリクエスト

UnionAir は現在、設計初期段階にあり、アーキテクチャや API 仕様が大幅に変更される可能性があります。

- バグ報告・機能要望・質問は [GitHub Issues](https://github.com/LeonAkasaka/UnionAir/issues) で受け付けています。
- 変更内容を Issue で相談する前にプルリクエストを作成しないでください。
- 現在プルリクエストを受け付けるのは、メンテナーと変更方針について合意済みの場合、または外部コントリビューション対象として明示された Issue に対応する場合のみです。
- 事前相談のないプルリクエストは、未公開の変更予定と競合する可能性があるため、詳細なレビューを行わずクローズする場合があります。

## 開発規約

開発規約はすべて [AGENTS.md](AGENTS.md) に集約されており、人間・AI を問わずすべてのコントリビューターに適用されます。要点:

- コードコメント・ドキュメント・コミットメッセージは**英語のみ**。日本語ドキュメントは英語原本の `.ja.md` 訳としてのみ存在します。
- API エンドポイントを追加・変更・削除した場合は、対応する `Documentation~/api/<category>.md` ページ、`[UnionAirEndpoint]` メタデータ、`CHANGELOG.md` を**同一コミットで**更新してください。
- 英語ドキュメントを変更した場合は、対応する `.ja.md` 訳を同一コミットで更新してください。
- 外部 NuGet 依存は追加しないでください — HTTP サーバは意図的に `HttpListener` ベースです。

## テスト

Unity に依存しないヘルパーの EditMode テストは `Tests/Editor` にあります。これらは利用側プロジェクトでは **コンパイルされません**。Unity がパッケージのテストアセンブリをビルドするのは、利用側が明示的にオプトインした場合だけだからです。実行するには、テスト用プロジェクトの `Packages/manifest.json` の `testables` にパッケージを追加してください。

```json
{
  "testables": ["com.leonakasaka.unionair"]
}
```

その後、**Window > General > Test Runner**(EditMode)から実行するか、UnionAir 自身の `POST /api/test-runs` で実行します。

カバー範囲は、Editor を動かさずに検証できるロジック — コンパイラメッセージの解析、パス正規化、ログカーソルの算術 — に意図的に限定しています。実際のコンパイル、domain reload、HTTP サーバに依存する挙動は引き続き手動での検証が必要です。

## バージョン範囲をまたいだ変更の検証

**CI はありません。** 宣言した下限は誰かが実際にコンパイルして初めて意味を持つため、プルリクエストを出す前に両端を手動で検証してください。

`package.json` の `unity` フィールドはメタデータにすぎません。宣言した最小バージョンより新しい API を呼んでいても Unity はビルドを通すため、このフィールドを読むだけでは何も証明できません。

1. **両端でコンパイルする。** 2022.3 のプロジェクトと、手元にある最新の Unity 6 のプロジェクトの双方にローカル依存としてパッケージを追加し、Console に `error CS` と **`warning CS`** が出ていないか確認します。範囲の上端に出る非推奨警告は、次の破壊的変更の予告です。
2. **下限で何が落ちるかを確認する。** 2022.3 と 2023.1 では `com.unity.test-framework` の既定バージョンが 1.4.0 未満のため、Test Runner アセンブリはスキップされます。この状態でも `GET /api/help` はエラーにならず、`/api/tests` と `/api/test-runs` のルートだけが存在しない形で応答する必要があります。プロジェクトに `"com.unity.test-framework": "1.4.6"` を追加して、これらが戻ることも確認してください。
3. **変更したエンドポイントを実際の Editor で動かす。** 本パッケージの大半はその方法でしか到達できません([既知の制約](#既知の制約) を参照)。
4. **テストを実行する。** [テスト](#テスト) を参照してください。

## リリース手順

`main` は統合ブランチであり、最新タグより先行します。リリースを識別するのはタグであるため、**一度 push したタグは決して動かさないでください**。利用者はタグを `packages-lock.json` に解決して記録しており、タグを動かすと記録自体は変わらないままその意味だけが変わってしまいます。

インストール手順に書かれた固定タグは、利用者が実際に入れるバージョンそのものであり、かつ静かに古くなる箇所です。記憶に頼らず、必ずこのリストで確認してください。

1. 上記の[バージョン範囲をまたいだ変更の検証](#バージョン範囲をまたいだ変更の検証)に従って `main` を検証します。リリースとは、宣言した下限が動作するという約束です。
2. `package.json` — `version` を更新します。
3. `CHANGELOG.md` — `[Unreleased]` を新しい `[x.y.z] - YYYY-MM-DD` 見出しに移し、ファイル末尾のリンク定義も更新します。
4. **すべてのインストール URL の固定タグを更新します。** 4 ファイル × 2 箇所の計 8 箇所です。
   - `README.md` と `README.ja.md` — Package Manager 用の Git URL と `manifest.json` の例。
   - `Documentation~/index.md` と `Documentation~/index.ja.md` — はじめにガイド内の同じ 2 箇所。

   `package.json` の `repository.url` は**対象外**です。これはバージョンではなくリポジトリを指すものであり、固定しません。
5. コミットし、`git tag -a vX.Y.Z` を作成します。
6. `git push origin main vX.Y.Z` を実行します。

UPM の Git URL は固定 ref しか受け付けず、バージョン範囲を指定する構文はありません。つまり読み手がどのバージョンに行き着くかを決めているのは手順 4 です。1 箇所でも更新し忘れると、その読み手には黙って前のリリースが渡ります。

## 既知の制約

- **CI はありません**。またコンパイル、domain reload、Play モード、HTTP サーバが関わる挙動には自動テストがありません。これらは Unity 6000.0+ プロジェクトで手動検証してください([はじめに](Documentation~/index.ja.md) を参照)。
- リクエストボディの JSON パースは軽量な独自リーダー(`Editor/Utils/RequestBodyReader.cs`)で、深いネストにエッジケースがあります。
- レスポンスの JSON シリアライズはエンドポイントごとの手書き実装です。共有シリアライザへのリファクタリングは歓迎します。

## セキュリティ問題

セキュリティに関する問題は、公開 Issue ではなく [SECURITY.ja.md](SECURITY.ja.md) の手順に従って報告してください。
